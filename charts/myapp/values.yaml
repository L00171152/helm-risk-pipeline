replicaCount: 1

image:
  repository: nginx
  tag: "1.29"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

serviceAccount:
  create: true
ingress:
  enabled: false
autoscaling:
  enabled: false